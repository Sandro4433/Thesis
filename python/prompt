This must have something to do with how parts are handled in my workspace. If parts are not near a Kit or container slot, they are considered "free". in that case their metric data is written with their part name . E.g Part_2 ....

but if they are close to slots their metric data is merged or added with the slot metric data which also includes orientation. I forgot how exactly that is done in the code, but just now I tried it with the update scene without post execution robot update ( I just made the config file post execution manually) and it worked. this bug must have something to do when the post execution updates, but doesnt link the part with the corrosponding slot properly. inside the config files it does say predicates "this part belongs to this slot" but the planner then Prints them parts in the sequence as "Part_xy" not "Slot_xy". With some parts it has worked like this but in some specific cases like in this experiment this is not handled correctly
